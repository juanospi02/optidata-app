/**
 * core/config.ts — URL base del backend
 *
 * ANTES DE LA EXPO: reemplaza el valor de API_URL con la URL que
 * te dé ngrok al correr:  ngrok http 4000
 *
 * Ejemplo:
 *   export const API_URL = 'https://abc123.ngrok-free.app';
 *
 * Para desarrollo local vuelve a:
 *   export const API_URL = 'http://localhost:4000';
 */
export const API_URL = '';
